import React from 'react';

import ComingSoon from '../ComingSoon/ComingSoon';

const subscriber = (props) => {
    return (
        <ComingSoon component="User Setting" />
    );
}

export default subscriber;