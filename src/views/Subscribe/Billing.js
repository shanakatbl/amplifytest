import React from 'react';

import ComingSoon from '../ComingSoon/ComingSoon';

const subscribe = (props) => {
    return (
        <ComingSoon component="Billing" />
    );
}

export default subscribe;